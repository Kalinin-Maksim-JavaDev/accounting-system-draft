spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:C:/temp/h2/sample-schema
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  liquibase:
    enabled: false